#pragma once


#include "Seedra/structures/gen2/TemplateManager.hpp"
#include "WoodlandMansionPieces.hpp"
#include "common/rng.hpp"
#include "common/timer.hpp"


class WoodlandMansion {

    /*
    void fuckingMakem() {
        DataFixer dataFixer;
        TemplateManager templateManager("structures", dataFixer);

        Timer timer;
        RNG rng(0);
        Pos3D origin{60, 110, 252};
        WoodlandMansionPieces::generateAt(
                templateManager,
                origin,
                Rotation::NONE,
                wm_pieces,
                rng
        );
        auto end = timer.getSeconds();
        std::cout << "Time to gen mansion: " << end << "\n";
    }
     */

};
