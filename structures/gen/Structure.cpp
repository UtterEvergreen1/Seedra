#include "Structure.hpp"


/*
template<int ARRAY_SIZE>
bool Structure<ARRAY_SIZE>::hasCollisionPiece(const BoundingBox& bbIn) {
    for (int i = 0; i < pieceArraySize; i++) {
        collisionChecks++;
        if (pieceArray[i].intersects(bbIn)) { return true; }
    }
    return false;
}


template<int ARRAY_SIZE>
StructureComponent* Structure<ARRAY_SIZE>::findCollisionPiece(const BoundingBox& bbIn) {
    for (int i = 0; i < pieceArraySize; i++) {
        collisionChecks++;
        if (pieceArray[i].intersects(bbIn)) { return &pieceArray[i]; }
    }
    return nullptr;
};


template class Structure<256>;
template class Structure<512>;
template class Structure<1024>;
 */